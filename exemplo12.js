const frutas = ['Maçã', 'Banana', 'Uva'];
    for (const fruta of [...frutas]) {
    frutas.shift();
     frutas.pop();
    frutas.push('limão', 'abacaxi', 'melancia', );



  console.log(frutas);
  break
}